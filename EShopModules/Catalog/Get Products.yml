info:
  name: Get Products
  type: http
  seq: 2

http:
  method: GET
  url: "{{eshop_url}}/product?pageIndex=0&pageSize=2"
  params:
    - name: pageIndex
      value: "0"
      type: query
    - name: pageSize
      value: "2"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
