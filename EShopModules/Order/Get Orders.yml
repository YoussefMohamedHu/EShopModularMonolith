info:
  name: Get Orders
  type: http
  seq: 2

http:
  method: GET
  url: "{{eshop_url}}/order?pageIndex=0&pageSize=10"
  params:
    - name: pageIndex
      value: "0"
      type: query
    - name: pageSize
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
