info:
  name: Get Basket
  type: http
  seq: 4

http:
  method: GET
  url: "{{eshop_url}}/basket/youssef"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
